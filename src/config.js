const config = {
  API_URL: 'https://fn-fasedrop-beta.azurewebsites.net',
  ENDPOINTS: {
    LOGIN: '/api/auth/sign-in',
    ALUMNO: '/api/alumno',
    NOTA: '/api/nota'
  }
};

export default config; 